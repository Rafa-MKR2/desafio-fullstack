import { Component } from '@angular/core';

/** Área do coordenador — será construída na próxima iteração (CRUD de aulas). */
@Component({
  selector: 'app-gestao-aulas',
  template: `
    <section class="pagina">
      <h1>Gestão de aulas</h1>
      <div class="cartao">
        🚧 Em construção — próxima iteração: CRUD de aulas (POST/PUT/DELETE
        /aulas) com formulário e validação.
      </div>
    </section>
  `,
})
export class GestaoAulasComponent {}
